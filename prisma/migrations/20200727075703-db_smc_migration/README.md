# Migration `20200727075703-db_smc_migration`

This migration has been generated by Airton Santos at 7/27/2020, 7:57:03 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `db_nexus_prisma2`.`User` (
`email` varchar(191) NOT NULL ,
`id` int NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  ,
`role` ENUM('USER', 'ADMIN') NOT NULL DEFAULT 'USER',
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `db_nexus_prisma2`.`Post` (
`authorId` int  ,
`content` varchar(191)  ,
`id` int NOT NULL  AUTO_INCREMENT,
`published` boolean NOT NULL DEFAULT false,
`title` varchar(191) NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `db_nexus_prisma2`.`Phone` (
`id` int NOT NULL  AUTO_INCREMENT,
`number` varchar(191)  ,
`description` varchar(191) NOT NULL ,
`userId` int  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `db_nexus_prisma2`.`User`(`email`)

CREATE UNIQUE INDEX `Phone.number` ON `db_nexus_prisma2`.`Phone`(`number`)

CREATE UNIQUE INDEX `Phone_userId` ON `db_nexus_prisma2`.`Phone`(`userId`)

ALTER TABLE `db_nexus_prisma2`.`Post` ADD FOREIGN KEY (`authorId`) REFERENCES `db_nexus_prisma2`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `db_nexus_prisma2`.`Phone` ADD FOREIGN KEY (`userId`) REFERENCES `db_nexus_prisma2`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

DROP TABLE `db_nexus_prisma2`.`_migration`;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200727075703-db_smc_migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,39 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model User {
+  email String  @unique
+  id    Int     @default(autoincrement()) @id
+  name  String?
+  phone Phone
+  posts Post[]
+  role Role @default(USER)
+}
+
+model Post {
+  authorId  Int?
+  content   String?
+  id        Int     @default(autoincrement()) @id
+  published Boolean @default(false)
+  title     String
+  author    User?   @relation(fields: [authorId], references: [id])
+}
+
+model Phone {
+  id          Int @default(autoincrement()) @id
+  number      String? @unique
+  description String
+  userId      Int?
+  owner       User?   @relation(fields: [userId], references: [id])
+}
+
+enum Role {
+  USER
+  ADMIN
+}
```



# Migration `20200724140118-user-post-phone`

This migration has been generated by Airton Santos at 7/24/2020, 2:01:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `db_nexus_prisma`.`User` (
`email` varchar(191) NOT NULL ,
`id` int NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `db_nexus_prisma`.`Post` (
`authorId` int  ,
`content` varchar(191)  ,
`id` int NOT NULL  AUTO_INCREMENT,
`published` boolean NOT NULL DEFAULT false,
`title` varchar(191) NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `db_nexus_prisma`.`Phone` (
`id` int NOT NULL  AUTO_INCREMENT,
`number` int  ,
`description` varchar(191)  ,
`ownerId` int  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `db_nexus_prisma`.`User`(`email`)

ALTER TABLE `db_nexus_prisma`.`Post` ADD FOREIGN KEY (`authorId`) REFERENCES `db_nexus_prisma`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `db_nexus_prisma`.`Phone` ADD FOREIGN KEY (`ownerId`) REFERENCES `db_nexus_prisma`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

DROP TABLE `db_nexus_prisma`.`_migration`;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200724140118-user-post-phone
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model User {
+  email String  @unique
+  id    Int     @default(autoincrement()) @id
+  name  String?
+  posts Post[]
+}
+
+model Post {
+  authorId  Int?
+  content   String?
+  id        Int     @default(autoincrement()) @id
+  published Boolean @default(false)
+  title     String
+  author    User?   @relation(fields: [authorId], references: [id])
+}
+
+model Phone {
+  id          Int @default(autoincrement()) @id
+  number      Int?
+  description String?
+  ownerId     Int?
+  owner       User?   @relation(fields: [ownerId], references: [id])
+}
```


